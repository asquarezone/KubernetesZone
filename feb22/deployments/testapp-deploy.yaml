---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: testapp-deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: testapp
  template: 
    metadata:
      labels:
        app: testapp
        ver: 1.0.1
    spec:
      containers:
        - name: testapp
          image: shaikkhajaibrahim/testapp:1.0.1
          ports:
            - containerPort: 80
              protocol: TCP
